[package]
name = "tests"
version = "0.1.0"
edition = "2021"

[features]

[dependencies]
ckb-testtool = "0.15.0"
serde_json = "1.0"
ckboost-shared = { path = "../libs/ckboost-shared" }
ckb_deterministic = { path = "../../../ckb_deterministic/ckb_deterministic" }
